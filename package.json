{
  "scripts": {
    "package": "npm run package:blink && npm run package:gecko",
    "package:blink": "cd extension && zip -r ../clear-radio-buttons.zip * && cd ..",
    "package:gecko": "cd extension && zip -r ../clear-radio-buttons.xpi * && cd .."
  }
}
